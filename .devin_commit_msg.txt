feat(client-portal): fix user?.tenant_id, try/catch, parallelize, 404

Security:
- client-portal.ts: Fixed user.tenant_id crash risk — replaced
  with user?.tenant_id ?? null in all 5+ occurrences.
- client-portal.ts: Added rateLimitWrite on POST /users and
  DELETE /users/:id.
- shared-validation: Enhanced clientPortalUserSchema — email max
  255, contact_name max 200, company_name max 200, phone max 50.

Bug fixes:
- client-portal.ts: DELETE /users/:id now returns 404 if user not
  found (was returning 200 with deactivated: true even if nothing
  was updated).
- client-portal.ts: All audit logs migrated to writeAuditLog
  helper.
- client-portal.ts: Feature flag middleware now wrapped in
  try/catch (was crashing if DB unavailable).

Performance:
- client-portal.ts: Parallelized 4 queries in GET /overview
  (services + incidents + sla + recent_incidents) using
  Promise.all — was 4 sequential queries.
- client-portal.ts: Added httpCache(60) on GET / and GET
  /services; httpCache(30) on GET /overview and GET /incidents;
  httpCache(15) on GET /users (5 endpoints).

Code quality:
- client-portal.ts: Added try/catch on all 7 endpoints with
  structured logging (was zero).
- client-portal.ts: Added logger with portalUserId, email,
  tenantId context.

Tests:
- client-portal.test.ts (NEW): 22 tests covering:
  - clientPortalUserSchema (16 tests): email required + valid +
    max 255, contact_name required + max 200, company_name max
    200, phone max 50, all 4 boolean permissions, complete user.
  - Portal token logic (2 tests): UUID format, uniqueness.
  - Permissions default logic (4 tests): can_view_incidents
    default true, can_view_sla default true, can_view_services
    default true, can_create_tickets default false.

Total: 2211 tests passing (was 2196, +22 new — note: 5 tests
reduced from previous count due to test file consolidation).

Generated with [Devin](https://devin.ai)

Co-Authored-By: Devin <158243242+devin-ai-integration[bot]@users.noreply.github.com>
