feat(branding): add try/catch, httpCache, rate limit, logger

Security:
- branding.ts: Added rateLimitApi on GET /:tenantSlug (public
  endpoint, was unprotected from abuse).

Performance:
- branding.ts: Added httpCache(300) on GET /:tenantSlug (5 min,
  public endpoint called on every login page load); httpCache(600)
  on GET / (10 min, static defaults).

Code quality:
- branding.ts: Added try/catch on GET /:tenantSlug with structured
  logging — returns default branding on error instead of crashing
  (public endpoint must not break the login page).
- branding.ts: Added logger with tenantSlug context.

Tests:
- branding.test.ts (NEW): 13 tests covering:
  - Default branding values (6 tests): primary_color hex, secondary
    color hex, null fields.
  - Tenant slug normalization logic (5 tests): spaces to hifens,
    multiple spaces, uppercase, no spaces, empty.
  - Branding response logic (2 tests): defaults when not found,
    tenant branding when found.

Total: 2116 tests passing (was 2103, +13 new).

Generated with [Devin](https://devin.ai)

Co-Authored-By: Devin <158243242+devin-ai-integration[bot]@users.noreply.github.com>
