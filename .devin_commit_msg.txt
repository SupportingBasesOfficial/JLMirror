feat(changes): fix 4 schemas, SQL injection, N+1, try/catch, rate limit, parallelize

Security (CRITICAL):
- shared-validation/src/index.ts: Fixed all 4 change management schemas to
  match DB migration. createChangeRequestSchema had `priority` as number
  (0-4) but DB is enum string (low/medium/high/critical); had `impact`
  field that doesn't exist in DB; `change_type` and `risk_level` were free
  strings but DB is enum; `description` was required but DB is nullable.
  updateChangeRequestSchema had `priority` as number; was missing
  `change_type`, `risk_level`, `assigned_to`, `planned_start_at`,
  `planned_end_at`, `impact_assessment`, `rollback_plan`,
  `rollback_status`, `implementation_notes`, `post_implementation_review`.
  createChangeTaskSchema had `change_request_id` instead of `change_id`
  (DB column); had `assignee_id` that doesn't exist in DB; `task_type`
  default was "manual" but DB enum is
  (pre_check/implementation/post_check/rollback). approveChangeSchema had
  `change_request_id` and `approved` fields that weren't used by the
  endpoint (uses :changeId from URL).
- changes.ts: Fixed SQL injection vulnerability in GET /calendar/month —
  `month` param was concatenated into startDate/endDate strings without
  validation. Added isValidMonthFormat() regex validation (YYYY-MM) to
  block injection attempts like `' OR 1=1 --`.
- changes.ts: Fixed user.sub crash risk — replaced with user?.sub ?? null
  in all 10+ occurrences.
- changes.ts: Added rateLimitWrite on POST /, PUT /:changeId,
  POST /:changeId/approve, POST /:changeId/reject,
  POST /:changeId/implement, POST /:changeId/complete,
  POST /:changeId/rollback, POST /:changeId/tasks,
  PUT /:changeId/tasks/:taskId (9 endpoints).

Performance:
- changes.ts: Parallelized 10 queries in GET /stats using Promise.all
  (was 9 sequential queries + 1 for recent + 1 for upcoming = 11 total).
- changes.ts: Parallelized 2 queries in GET /:changeId (approvals + tasks
  were sequential).
- changes.ts: Added httpCache(30) on GET /, GET /stats, GET /calendar/month
  (60s for calendar).

Code quality:
- changes.ts: Added try/catch on all 13 endpoints with structured logging.
- changes.ts: Added logger with tenantId, changeId, taskId context. Logs
  change creation, approval, rejection, implementation start/complete,
  rollback.
- changes.ts: Added safeJsonBody() for safe request body parsing.
- changes.ts: Added validation error details (parsed.error.flatten()) to
  all error responses.
- changes.ts: Fixed POST /:changeId/reject to verify change exists before
  rejecting (was creating approval record for non-existent change).
- changes.ts: Fixed POST /:changeId/tasks to verify change request exists
  before creating task (was creating orphan tasks).
- changes.ts: Fixed PUT /:changeId, POST /:changeId/complete,
  POST /:changeId/rollback, PUT /:changeId/tasks/:taskId to return 404
  when not found (was returning success regardless).

Tests:
- changes.test.ts (NEW): 86 tests covering all 4 Zod schemas
  (createChangeRequest, updateChangeRequest, createChangeTask,
  approveChange) with all enum values, defaults, and edge cases; status
  transition logic (approve/implement); task status logic (started_at/
  completed_at triggers); rollback_status logic; success rate calculation;
  month format validation (SQL injection prevention).

Total: 881 tests passing (was 795, +86 new).

Generated with [Devin](https://devin.ai)

Co-Authored-By: Devin <158243242+devin-ai-integration[bot]@users.noreply.github.com>
