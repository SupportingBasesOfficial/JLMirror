feat(profile): fix user?.sub, safeJsonBody, try/catch, enhance schemas

Security:
- profile.ts: Fixed user.sub crash risk — replaced with user?.sub ??
  null in all 10+ occurrences.
- profile.ts: Fixed user.roles[0] crash — replaced with
  user?.roles?.length ? user.roles[0] : null.
- profile.ts: Added rateLimitWrite on 5 write endpoints (PUT /,
  PUT /avatar, PUT /preferences, DELETE /sessions/:id,
  DELETE /sessions).
- profile.ts: Replaced c.req.json() with safeJsonBody() in 3
  endpoints (PUT /, PUT /avatar, PUT /preferences).
- shared-validation: Enhanced updateProfileSchema — display_name
  max 200, bio max 2000, phone max 50, location max 200, timezone
  max 100, locale max 20, job_title max 200, department max 200,
  skills items max 100 + array max 50, social_links values max 500,
  email max 255, full_name max 255.
- shared-validation: Enhanced updatePreferencesSchema —
  quiet_hours_start/end regex /^([01]\d|2[0-3]):[0-5]\d$/ (HH:MM).
- shared-validation: Enhanced updateAvatarSchema — avatar_url max
  2000, avatar_initials max 10, avatar_color regex
  /^#[0-9a-fA-F]{3,8}$/ (hex color).

Bug fixes:
- profile.ts: All validation errors now return parsed.error.flatten().
- profile.ts: DELETE /sessions/:sessionId now returns 404 if not
  found.
- profile.ts: Security log inserts now wrapped in try/catch (was
  crashing silently if log table unavailable).

Performance:
- profile.ts: Parallelized 2 queries in GET / (profile + user data)
  using Promise.all.
- profile.ts: Added httpCache(15) on GET /, GET /preferences,
  GET /sessions, GET /security/log (4 endpoints).

Code quality:
- profile.ts: Added try/catch on all 8 endpoints with structured
  logging.
- profile.ts: Added logger with userId, tenantId, sessionId context.

Tests:
- profile.test.ts (NEW): 56 tests covering:
  - updateProfileSchema (21 tests): display_name/bio/phone/location/
    timezone/locale/job_title/department max length, skills array
    max 50 + item max 100, social_links value max 500, email
    validation.
  - updatePreferencesSchema (18 tests): notification booleans,
    digest_frequency enum, quiet_hours HH:MM regex, theme/density
    enums, sidebar_collapsed, dashboard_layout.
  - updateAvatarSchema (10 tests): avatar_url URL + max 2000,
    avatar_initials max 10, avatar_color hex regex (#FFF, #FF5733,
    invalid "red", missing #).
  - Initials generation logic (4 tests): name, 1-char, email,
    fallback.
  - Limit pagination logic (3 tests).

Total: 1983 tests passing (was 1927, +56 new).

Generated with [Devin](https://devin.ai)

Co-Authored-By: Devin <158243242+devin-ai-integration[bot]@users.noreply.github.com>
