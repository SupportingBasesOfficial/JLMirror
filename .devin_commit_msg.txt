feat(assets): fix IDOR, schema mismatch, parallelize queries, add rate limiting

Security (CRITICAL):
- assets.ts: Fixed IDOR in GET /:id — asset_changes query now filters
  by tenant_id via INNER JOIN with assets table (was only filtering by
  asset_id, allowing cross-tenant change history access).
- assets.ts: Added ownership verification in POST /:id/licenses —
  checks asset belongs to tenant before adding license (was missing,
  allowing license creation on other tenants' assets).
- assets.ts: Added rateLimitWrite on POST /, PUT /:id, DELETE /:id,
  POST /:id/licenses, PUT /:id/licenses/:licenseId,
  DELETE /:id/licenses/:licenseId.
- assets.ts: DELETE /:id now verifies asset exists and belongs to
  tenant before deleting (was returning deleted:true even if nothing
  was deleted).
- shared-validation: Fixed createAssetSchema — removed dead field
  'type' (was required but never used in INSERT). Made asset_tag and
  asset_type required (was optional but used in INSERT). Added enum
  validation for status and criticality. Added max length constraints.
  Aligned updateAssetSchema with fieldMap (added asset_tag, asset_type,
  category, and all missing fields).

Performance:
- assets.ts: Parallelized 6 sequential queries in GET /stats with
  Promise.all. Added httpCache(30).
- assets.ts: Parallelized 3 sequential queries in GET /:id (licenses,
  changes, children) with Promise.all.

Code quality:
- assets.ts: Added try/catch on all endpoints with graceful error
  responses and structured logging via @repo/logger.
- assets.ts: Added logger with tenantId, assetId, and error context.

Tests:
- assets.test.ts (NEW): 48 tests covering all Zod schemas (createAsset,
  updateAsset, createLicense, updateLicense), filter logic, warranty
  classification, and edge cases (empty tenant, limit validation,
  null fields).

Total: 383 tests passing (was 335, +48 new).

Generated with [Devin](https://devin.ai)

Co-Authored-By: Devin <158243242+devin-ai-integration[bot]@users.noreply.github.com>
