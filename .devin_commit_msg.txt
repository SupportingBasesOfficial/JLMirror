feat(backup): fix command injection, schemas, try/catch, parallelize, rate limit

Security (CRITICAL):
- backup.ts: Fixed command injection vulnerability in POST /jobs/:id/run and
  POST /restore. tar/ssh commands were using unsanitized user input
  (source_path, destination_path, target_host) directly in shell commands.
  Added isSafePath() and isSafeHostname() helpers that block shell
  metacharacters (;, |, `, $, &, etc.), path traversal (..), and command
  substitution ($(), ``). Applied on POST /jobs (creation), PUT /jobs
  (update), POST /jobs/:id/run (execution), and POST /restore.
- backup.ts: Fixed calculateFileChecksum to use Node.js crypto.createHash
  with streaming (createReadStream) instead of execSync(`sha256sum ...`)
  which was vulnerable to command injection via filePath.
- backup.ts: Revalidates paths from DB before using in shell commands
  (defense in depth — even if DB data was tampered).
- shared-validation/src/index.ts: Fixed createBackupJobSchema — compression
  was boolean but DB expects string enum (none/gzip/zstd/bzip2/lz4).
  backup_type was missing 'snapshot'. destination_type was missing
  'azure_blob' and 'gcs'. createRestoreSchema required job_id but endpoint
  uses snapshot_id; was missing required target_host and target_path.
- backup.ts: Fixed user.sub crash risk — replaced with user?.sub ?? null
  in all 8 occurrences (POST /jobs, PUT /jobs, DELETE /jobs, POST /jobs/:id/run,
  POST /snapshots/:id/verify, POST /restore).
- backup.ts: Added rateLimitWrite on POST /jobs, PUT /jobs/:id,
  DELETE /jobs/:id, POST /jobs/:id/run, POST /snapshots/:id/verify,
  POST /restore (6 endpoints).

Performance:
- backup.ts: Parallelized 2 queries in GET / using Promise.all.
- backup.ts: Parallelized 2 queries in GET /jobs/:id using Promise.all
  (snapshots + restores were sequential).
- backup.ts: Parallelized 3 queries in GET /stats using Promise.all
  (jobs + snapshots + recent were sequential).
- backup.ts: Added httpCache(30) on GET /jobs, GET /snapshots,
  GET /restores, GET /stats.

Code quality:
- backup.ts: Added try/catch on all 12 endpoints with generic Portuguese
  error messages and structured logging.
- backup.ts: Added logger with tenantId, jobId, snapshotId, restoreId
  context. Logs backup failures, successes, and restore outcomes.
- backup.ts: Added safeJsonBody() for safe request body parsing.
- backup.ts: Added validation error details (parsed.error.flatten()) to
  all error responses.
- backup.ts: Fixed error_message not being stored on backup_snapshots
  and backup_restores when operations fail (now truncates to 5000 chars).
- backup.ts: Fixed DELETE /jobs/:id and PUT /jobs/:id to return 404 when
  job not found (was returning success regardless).
- backup.ts: Removed unused imports (execSync, type imports).

Tests:
- backup.test.ts (NEW): 79 tests covering all 3 Zod schemas
  (createBackupJob, updateBackupJob, createRestore) with all enum values,
  defaults, and edge cases; isSafePath SSRF/command injection protection
  (17 path tests covering traversal, semicolons, pipes, backticks, command
  substitution, globs, newlines); isSafeHostname protection (12 host tests);
  compression flag logic (gzip vs none vs zstd); restore logic (file
  extension detection, overwrite flag, remote vs localhost, checksum
  verification conditions).

Total: 732 tests passing (was 653, +79 new).

Generated with [Devin](https://devin.ai)

Co-Authored-By: Devin <158243242+devin-ai-integration[bot]@users.noreply.github.com>
