feat(discovery): fix N+1 subqueries, try/catch, schema, parallelize

Security:
- discovery.ts: Fixed user.tenant_id and user.sub crash risk —
  replaced with user?.tenant_id ?? null and user?.sub ?? null in
  all 7+ occurrences.
- discovery.ts: Added rateLimitWrite on POST /sessions, POST
  /sessions/:id/run, DELETE /sessions/:id, POST /devices/:id/import
  (4 endpoints).
- shared-validation: Enhanced discoverySessionSchema — name max
  200, ip_ranges max 100 entries with each max 50 chars,
  snmp_communities max 20 entries with each max 100 chars,
  snmp_ports 1-65535 range with max 20 entries, snmp_timeout_ms
  max 60000, snmp_retries max 10.

Bug fixes:
- discovery.ts: DELETE /sessions/:id now returns 404 if session
  not found.
- discovery.ts: All audit logs migrated to writeAuditLog helper.

Performance:
- discovery.ts: Fixed N+1 subqueries in GET /sessions — replaced
  correlated subqueries (device_count + link_count per row) with
  LEFT JOIN + aggregation (2 subqueries → 2 JOINs in single
  query).
- discovery.ts: Parallelized 2 queries in GET /topology (devices
  + links) using Promise.all — was 2 sequential queries.
- discovery.ts: Parallelized 2 operations in POST /devices/:id/
  import (vincula + audit log) using Promise.all.
- discovery.ts: Added httpCache(30) on GET /, GET /sessions, GET
  /sessions/:id/devices, GET /sessions/:id/links, GET
  /sessions/:id/topology (5 endpoints).

Code quality:
- discovery.ts: Added try/catch on all 8 endpoints with structured
  logging (was only on /run inner block).
- discovery.ts: Added logger with sessionId, name, devicesFound,
  linksFound, discoveredId, newDeviceId, ip, tenantId context.

Tests:
- discovery.test.ts (NEW): 34 tests covering:
  - discoverySessionSchema (23 tests): name required + max 200,
    ip_ranges required + max 100 + each max 50 + empty rejected,
    snmp_communities max 20, snmp_ports 1-65535 + max 20,
    snmp_timeout_ms max 60000, snmp_retries max 10, all 3 boolean
    flags, complete session.
  - Defaults logic (7 tests): snmp_communities, snmp_ports,
    snmp_timeout_ms, snmp_retries, use_snmp, use_lldp, use_arp.
  - Topology logic (2 tests): devices → nodes conversion with
    hostname fallback, links → edges conversion.
  - Import logic (3 tests): hostname default generation, hostname
    when provided, device_type default.

Total: 2327 tests passing (was 2293, +34 new).

Generated with [Devin](https://devin.ai)

Co-Authored-By: Devin <158243242+devin-ai-integration[bot]@users.noreply.github.com>
