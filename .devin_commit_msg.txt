feat(settings): fix 40+ missing schema fields, user?.sub, try/catch, parallelize

Security (CRITICAL):
- shared-validation: Fixed updateTenantSettingsSchema — was only
  defining 4 fields (settings, smtp_password_encrypted,
  telegram_bot_token, ip_whitelist) but the route's fieldMap had 40+
  fields (company_name, logo_url, smtp_host, smtp_port, etc.). Since
  Zod strips unknown fields, ALL 40+ fields were silently ignored —
  users could not update company_name, smtp_host, max_users, or any
  other setting. Now includes all 40+ fields with proper validation
  (max length, hex color regex, port range, int min/max).
- shared-validation: Enhanced testSmtpSchema — removed duplicate
  fields (smtp_user/smtp_username, smtp_pass/smtp_password_encrypted,
  smtp_from/smtp_from_email, smtp_to/test_email), added max length,
  test_email is now required (was optional).
- shared-validation: Added toggleModuleSchema and
  toggleModuleVisibilitySchema for module endpoints (was inline
  validation).
- settings.ts: Fixed user.sub crash risk — replaced with user?.sub ??
  null in all 6+ occurrences.
- settings.ts: Added rateLimitWrite on 6 write endpoints (PUT /,
  POST /test-smtp, PUT /modules/:key, PUT /modules/:key/visibility,
  PUT /modules/:key/client).
- settings.ts: Replaced c.req.json() with safeJsonBody() in 5
  endpoints.

Bug fixes:
- settings.ts: All validation errors now return parsed.error.flatten().
- settings.ts: All module audit logs migrated to writeAuditLog helper.
- settings.ts: Module endpoints now use toggleModuleSchema and
  toggleModuleVisibilitySchema instead of inline validation.

Performance:
- settings.ts: Parallelized 5 queries in GET /usage (devices, users,
  api_keys, webhooks, scheduled_tasks) using Promise.all — was 6
  sequential queries.
- settings.ts: Added httpCache(15) on GET /, GET /modules;
  httpCache(30) on GET /usage (3 endpoints).

Code quality:
- settings.ts: Added try/catch on all 8 endpoints with structured
  logging.
- settings.ts: Added logger with tenantId, moduleKey, host context.

Tests:
- settings.test.ts (NEW): 43 tests covering:
  - updateTenantSettingsSchema (20 tests): company_name max 200,
    logo_url URL, primary_color hex regex, smtp_port range 1-65535,
    max_devices non-negative, password_min_length 4-128,
    session_timeout_minutes, require_mfa, ip_whitelist, multi-field.
  - testSmtpSchema (8 tests): smtp_host required, smtp_port required
    + range, test_email required + email validation, SSL/TLS.
  - toggleModuleSchema (4 tests): enabled boolean required.
  - toggleModuleVisibilitySchema (4 tests): client_visible boolean.
  - Module key validation logic (3 tests): module_ prefix.
  - SMTP port/protocol matching logic (4 tests): 465+SSL, 587+TLS,
    SSL+TLS exclusivity, no warning when correct.

Total: 2026 tests passing (was 1983, +43 new).

Generated with [Devin](https://devin.ai)

Co-Authored-By: Devin <158243242+devin-ai-integration[bot]@users.noreply.github.com>
