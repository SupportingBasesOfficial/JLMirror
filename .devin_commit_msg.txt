feat(push): fix missing auth, user?.sub, try/catch, enhance schemas

Security:
- push.ts: Fixed 6 endpoints without requirePermission — GET /,
  GET /vapid-public-key, POST /subscribe, POST /unsubscribe,
  GET /subscriptions, DELETE /subscriptions/:id, POST /test were
  all accessible without authentication. Added requirePermission
  to all 7 endpoints.
- push.ts: Fixed user.tenant_id and user.sub crash risk — replaced
  with user?.tenant_id ?? null and user?.sub ?? null in all 6+
  occurrences.
- push.ts: Added rateLimitWrite on 5 write endpoints (POST /subscribe,
  POST /unsubscribe, DELETE /subscriptions/:id, POST /test,
  POST /broadcast).
- shared-validation: Enhanced pushSubscribeSchema — endpoint max 2000,
  keys.p256dh max 500, keys.auth max 500, device_type max 100,
  user_agent max 500.
- shared-validation: Enhanced pushUnsubscribeSchema — endpoint max 2000.
- shared-validation: Enhanced pushBroadcastSchema — title max 200,
  message max 2000.

Bug fixes:
- push.ts: DELETE /subscriptions/:id now returns 404 if not found.
- push.ts: POST /subscribe now returns 500 if DB insert fails (was
  returning id: undefined).
- push.ts: All audit logs migrated to writeAuditLog helper.

Performance:
- push.ts: Added httpCache(60) on GET /; httpCache(300) on
  GET /vapid-public-key; httpCache(15) on GET /subscriptions
  (3 endpoints).

Code quality:
- push.ts: Added try/catch on all 8 endpoints with structured logging.
- push.ts: Added logger with userId, tenantId, subId context.

Tests:
- push.test.ts (NEW): 23 tests covering:
  - pushSubscribeSchema (13 tests): endpoint URL validation, max
    length, keys required (p256dh + auth), device_type max 100,
    user_agent max 500.
  - pushUnsubscribeSchema (3 tests): endpoint required, max length.
  - pushBroadcastSchema (5 tests): title + message required, max
    length.
  - Push notification payload logic (2 tests): test payload
    construction, broadcast payload construction.

Total: 1900 tests passing (was 1877, +23 new).

Generated with [Devin](https://devin.ai)

Co-Authored-By: Devin <158243242+devin-ai-integration[bot]@users.noreply.github.com>
