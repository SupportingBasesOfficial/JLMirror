feat(notifications): fix 5 schemas, N+1 query, try/catch, rate limit, parallelize

Security (CRITICAL):
- shared-validation/src/index.ts: Fixed all 5 notification schemas to match
  DB migration. createChannelSchema had `type` field instead of
  `channel_type`; was missing `discord`, `pagerduty`, `web_push` in enum;
  had `whatsapp` which doesn't exist in DB. createRuleSchema had
  `channel_id` (singular) instead of `channel_ids` (array); had
  `conditions` field that doesn't exist in DB; had `severity_filter` as
  array instead of string enum; `event_source` and `event_category` were
  optional but DB is NOT NULL. updateRuleSchema only allowed updating
  `name`, `conditions`, `is_active` — was missing `description`,
  `event_source`, `event_category`, `severity_filter`, `channel_ids`,
  `template_subject`, `template_body`, `cooldown_minutes`.
  sendNotificationSchema had `event_source`, `event_category`, `severity`
  as optional but DB is NOT NULL; `severity` was free string instead of
  enum; had `channel_id` and `to` fields that don't map to DB columns.
- notifications.ts: Fixed user.sub crash risk — replaced with user?.sub
  ?? null in all 8 occurrences.
- notifications.ts: Added rateLimitWrite on POST /channels, PUT
  /channels/:id, DELETE /channels/:id, POST /channels/:id/test,
  POST /rules, PUT /rules/:id, DELETE /rules/:id, POST /send
  (8 endpoints).

Performance:
- notifications.ts: Parallelized 2 queries in GET / using Promise.all.
- notifications.ts: Parallelized 4 queries in GET /stats using Promise.all
  (channels + rules + log + byCategory were sequential).
- notifications.ts: Eliminated N+1 query in POST /send — was querying
  each channel individually inside nested loop. Now fetches all channels
  in a single query using `WHERE id = ANY($1::uuid[])` and builds a Map
  for O(1) lookup.
- notifications.ts: Added httpCache(30) on GET /channels, GET /rules,
  GET /log, GET /stats.

Code quality:
- notifications.ts: Added try/catch on all 12 endpoints with generic
  Portuguese error messages and structured logging.
- notifications.ts: Added logger with tenantId, channelId, ruleId
  context. Logs channel creation, rule creation, notification sends,
  and channel tests.
- notifications.ts: Added safeJsonBody() for safe request body parsing.
- notifications.ts: Added validation error details (parsed.error.flatten())
  to all error responses.
- notifications.ts: Fixed DELETE /channels/:id, DELETE /rules/:id,
  PUT /channels/:id, PUT /rules/:id to return 404 when not found
  (was returning success regardless).
- notifications.ts: Removed information disclosure — error message in
  /channels/:id/test was leaking deliveryResult.error to client;
  replaced with generic "Falha no teste do canal".

Tests:
- notifications.test.ts (REWRITTEN): 70 tests covering all 5 Zod schemas
  (createChannel, updateChannel, createRule, updateRule, sendNotification)
  with all enum values, defaults, and edge cases; cooldown logic;
  template fallback logic (template_subject vs data.subject); severity
  filter matching logic (all/info/warning/critical). Removed 7 obsolete
  tests that used removed `type` field and `whatsapp` channel type.

Total: 795 tests passing (was 732, +63 net new).

Generated with [Devin](https://devin.ai)

Co-Authored-By: Devin <158243242+devin-ai-integration[bot]@users.noreply.github.com>
