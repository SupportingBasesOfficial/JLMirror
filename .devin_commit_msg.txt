feat(firewall): fix Command Injection, user?.sub, safeJsonBody, try/catch, 404s

Security (CRITICAL):
- firewall.ts: Fixed Command Injection in execSshCommand — replaced
  exec() with execFile() that doesn't interpret shell metacharacters.
  Before: exec(`ssh -o StrictHostKeyChecking=no ${host} "${command}"`)
  allowed injection via host or command. Now: execFile("ssh", [args])
  with no shell interpretation.
- firewall.ts: Removed StrictHostKeyChecking=no — was allowing
  man-in-the-middle attacks. Now uses BatchMode=yes for non-interactive.
- firewall.ts: Fixed Command Injection in POST /apply — replaced
  exec(cmd) with execFile(cmd, args) using separate argument arrays.
  Commands now built via buildIptablesArgs/buildNftablesArgs/buildUfwArgs
  which return string[] instead of interpolated strings.
- firewall.ts: Fixed user.sub crash risk — replaced with user?.sub ??
  null in all 6+ occurrences.
- firewall.ts: Added rateLimitWrite on 5 write endpoints (POST /rules,
  PUT /rules/:id, DELETE /rules/:id, POST /dry-run, POST /apply).
- firewall.ts: Replaced c.req.json() with safeJsonBody() in 4 endpoints
  (POST /rules, PUT /rules/:id, POST /dry-run, POST /apply).
- shared-validation: Enhanced updateFirewallRuleSchema — was missing 9
  fields (backend, source_ip, source_port, destination_ip,
  destination_port, interface_in, interface_out, state, priority,
  is_enabled). Now supports all fields with same validation as create.

Bug fixes:
- firewall.ts: DELETE /rules/:id now returns 404 if not found.
- firewall.ts: PUT /rules/:id now returns 404 if not found after update,
  and 400 if no fields to update.
- firewall.ts: All validation errors now return parsed.error.flatten().
- firewall.ts: All audit logs migrated to writeAuditLog helper.

Performance:
- firewall.ts: Parallelized 2 queries in GET /rules/:id (rule +
  versions) using Promise.all.
- firewall.ts: Added httpCache(30) on GET /, GET /hosts;
  httpCache(15) on GET /rules, GET /rules/:id, GET /changes.

Code quality:
- firewall.ts: Added try/catch on all 9 endpoints with structured logging.
- firewall.ts: Added logger with ruleId, host, tenantId context.
- firewall.ts: Refactored command building into separate functions
  (buildIptablesArgs, buildNftablesArgs, buildUfwArgs) returning arrays.

Tests:
- firewall.test.ts (NEW): 56 tests covering:
  - createFirewallRuleSchema (28 tests): name, host shell injection
    prevention, action/protocol/backend/chain enums, defaults, source_ip/
    destination_port/interface_in shell injection prevention.
  - updateFirewallRuleSchema (12 tests): partial updates, all 9 new
    fields, priority range (0-32767), host shell injection.
  - applyFirewallSchema (8 tests): device_id required, rule_ids min 1,
    dry_run default, host shell injection.
  - iptables args building (3 tests): simple rule, complete rule,
    array verification (no string interpolation).
  - Limit pagination (3 tests), version bump (2 tests).

Total: 1621 tests passing (was 1565, +56 new).

Generated with [Devin](https://devin.ai)

Co-Authored-By: Devin <158243242+devin-ai-integration[bot]@users.noreply.github.com>
