feat(zabbix): fix user-host-groups try/catch, 404, audit, redact tests

Security:
- zabbix/admin.ts: Fixed user.tenant_id and user.sub crash risk in
  4 user-host-groups endpoints — replaced with user?.tenant_id ??
  null and user?.sub ?? null.
- zabbix/admin.ts: DELETE /user-host-groups/:id now returns 404 if
  assignment not found.
- zabbix/admin.ts: Added writeAuditLog on POST /user-host-groups
  (assign) and DELETE /user-host-groups/:id (unassign) — was zero
  audit logs for DB-direct operations.

Bug fixes:
- zabbix/admin.ts: Added try/catch on GET /user-host-groups (was
  missing — unhandled DB error would crash).
- zabbix/admin.ts: Added try/catch on DELETE /user-host-groups/:id
  (was missing).
- zabbix/admin.ts: Added try/catch on GET /user-host-groups/by-
  group/:groupId (was missing).

Code quality:
- zabbix/admin.ts: Added logger with assignmentId, tenantId,
  groupId context on user-host-groups endpoints.
- zabbix/admin.ts: Imported writeAuditLog from lib/audit.js.

Tests:
- zabbix-shared.test.ts (NEW): 30 tests covering:
  - redactSensitiveFields (18 tests): passwd, password, secret,
    private_key, encryption_key, connection_string, value with
    type=1 (macro secreta), value with type=0 (not redacted),
    non-sensitive fields preserved, nested objects, arrays of
    objects, primitives, null/undefined, empty array/object,
    case-insensitive (PASSWD, Password).
  - Response helpers (3 tests): configNotFoundResponse,
    accessDeniedResponse, validationErrorResponse.
  - IDOR protection logic (5 tests): global admin always passes,
    tenant passes when host belongs to group, tenant rejected
    when host doesn't belong, tenant rejected when host has no
    groups, global admin passes with no groups.
  - Batch ownership verification (4 tests): all belong, one
    doesn't belong, empty list, host not in map.

Total: 2410 tests passing (was 2380, +30 new).

Generated with [Devin](https://devin.ai)

Co-Authored-By: Devin <158243242+devin-ai-integration[bot]@users.noreply.github.com>
