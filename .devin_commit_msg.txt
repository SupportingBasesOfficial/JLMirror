feat(status-page): fix user?.tenant_id, try/catch, schema, parallelize

Security:
- status-page.ts: Fixed user.tenant_id and user.sub crash risk —
  replaced with user?.tenant_id ?? null and user?.sub ?? null in
  all 3+ occurrences.
- status-page.ts: Added rateLimitWrite on PUT /admin/config.
- shared-validation: Enhanced statusPageConfigSchema — slug max 100
  + regex message, page_title max 200, company_name max 200,
  logo_url URL validation + max 2000, primary_color hex regex
  + max 20, days_of_history max 365, support_email email validation
  + max 255, support_url URL validation + max 2000.

Bug fixes:
- status-page.ts: All audit logs migrated to writeAuditLog helper.

Performance:
- status-page.ts: Parallelized 4 queries in GET /:slug (services +
  activeIncidents + history + maintenance) using Promise.all — was
  4 sequential queries.
- status-page.ts: Added httpCache(60) on GET /:slug (public, rarely
  changes); httpCache(15) on GET /admin/config (2 endpoints).

Code quality:
- status-page.ts: Added try/catch on all 3 endpoints with structured
  logging.
- status-page.ts: Added logger with slug, tenantId, pageId context.

Tests:
- status-page.test.ts (NEW): 31 tests covering:
  - statusPageConfigSchema (26 tests): slug regex (lowercase,
    numbers, hifens only) + max 100, company_name required + max
    200, page_title max 200, logo_url URL validation, primary_color
    hex regex (#fff, #0d9488, invalid "teal"), days_of_history 1-365,
    support_email email validation, support_url URL validation,
    is_published boolean, complete config.
  - Overall status logic (5 tests): all operational, one degraded,
    down takes priority over degraded, empty list, all down.

Total: 2093 tests passing (was 2072, +21 net new).

Generated with [Devin](https://devin.ai)

Co-Authored-By: Devin <158243242+devin-ai-integration[bot]@users.noreply.github.com>
