feat(rbac): add requirePermission to GET endpoints, fix user?.sub, schemas

Security (CRITICAL):
- rbac.ts: Added requirePermission("tenant:settings:read") on 4
  GET endpoints (GET /, GET /permissions, GET /roles,
  GET /roles/:id/permissions) — were completely unprotected, any
  authenticated user could list all permissions and roles.
- rbac.ts: Fixed user.sub crash risk — replaced with user?.sub ??
  null in all 3+ occurrences.
- rbac.ts: Added rateLimitWrite on 2 write endpoints (POST /roles,
  PUT /roles/:id/permissions).
- rbac.ts: Replaced c.req.json() with safeJsonBody() in 2
  endpoints.
- rbac.ts: POST /roles now checks for duplicate key before insert
  — returns 409 CONFLICT if key already exists (was returning
  generic 500).
- rbac.ts: PUT /roles/:id/permissions now validates role exists
  and is not a system role — returns 404 if not found, 403 if
  system role (was silently updating non-existent or system roles).
- shared-validation: Enhanced createRoleSchema — name max 200,
  description max 2000, key regex /^[\w-]+$/ + max 100,
  permissions UUID validation + max 500.
- shared-validation: Fixed assignRolePermissionsSchema — removed
  duplicate `permissions` field (was duplicating `permission_ids`),
  permission_ids now UUID validation + max 500.

Bug fixes:
- rbac.ts: All validation errors now return parsed.error.flatten().
- rbac.ts: All audit logs migrated to writeAuditLog helper.
- rbac.ts: GET /me/permissions now uses user?.sub ?? null and
  user.roles ?? [].

Performance:
- rbac.ts: Added httpCache(60) on GET /, GET /permissions,
  GET /roles, GET /me/permissions; httpCache(30) on
  GET /roles/:id/permissions (5 endpoints).

Code quality:
- rbac.ts: Added try/catch on all 6 endpoints with structured
  logging.
- rbac.ts: Added logger with roleId, tenantId, userId context.

Tests:
- rbac.test.ts (NEW): 19 tests covering:
  - createRoleSchema (11 tests): name required + max 200,
    description max 2000, key regex + max 100, permissions UUID
    validation + default empty, multiple UUIDs.
  - assignRolePermissionsSchema (4 tests): permission_ids UUID
    validation, default empty, multiple UUIDs.
  - Role key generation logic (2 tests): uses key if provided,
    falls back to name.
  - System role protection logic (2 tests): system role cannot
    be modified, custom role can.

Total: 2103 tests passing (was 2093, +10 net new).

Generated with [Devin](https://devin.ai)

Co-Authored-By: Devin <158243242+devin-ai-integration[bot]@users.noreply.github.com>
