feat(itsm): fix user?.sub, try/catch, URL validation, auth_type enum

Security:
- itsm.ts: Fixed user.tenant_id and user.sub crash risk —
  replaced with user?.tenant_id ?? null and user?.sub ?? null in
  all 8+ occurrences.
- itsm.ts: Added rateLimitWrite on POST /connectors, DELETE
  /connectors/:id, POST /connectors/:id/test, POST
  /connectors/:id/create-ticket (4 endpoints).
- shared-validation: Enhanced itsmConnectorSchema — base_url now
  requires valid URL with HTTP/HTTPS protocol (SSRF prevention),
  auth_type changed from string to enum (api_key/basic/bearer/
  oauth2), sync_direction changed to enum (inbound/outbound/
  bidirectional), oauth_token_url requires valid URL, all string
  fields have max length (name 200, base_url 2000, api_key 500,
  username 200, password 500, bearer_token 2000, oauth_client_id
  200, oauth_client_secret 500).
- shared-validation: Enhanced itsmCreateTicketSchema — title max
  500, description max 10000, source_id max 200, source_type max
  100.

Bug fixes:
- itsm.ts: DELETE /connectors/:id now returns 404 if connector
  not found.
- itsm.ts: All audit logs migrated to writeAuditLog helper.

Performance:
- itsm.ts: Parallelized 5 queries in GET /stats (connectors +
  syncs + success + failed + byType) using Promise.all — was 5
  sequential queries.
- itsm.ts: Parallelized 2 queries in POST /create-ticket (log
  insert + connector status update) using Promise.all.
- itsm.ts: Added httpCache(30) on GET /, GET /connectors, GET
  /sync-log; httpCache(60) on GET /stats (4 endpoints).

Code quality:
- itsm.ts: Added try/catch on all 8 endpoints with structured
  logging (was zero).
- itsm.ts: Added logger with connectorId, name, connector_type,
  success, externalId, tenantId context.

Tests:
- itsm.test.ts (NEW): 32 tests covering:
  - itsmConnectorSchema (21 tests): name required + max 200, all
    5 connector_type values, base_url URL validation + HTTP/HTTPS
    + reject FTP, all 4 auth_type values, all 3 sync_direction
    values, oauth_token_url validation, complete connector.
  - itsmCreateTicketSchema (11 tests): title required + max 500,
    description max 10000, all 3 severity values, invalid
    severity rejected, extra_fields.

Total: 2243 tests passing (was 2211, +32 new).

Generated with [Devin](https://devin.ai)

Co-Authored-By: Devin <158243242+devin-ai-integration[bot]@users.noreply.github.com>
