feat(drift): fix N+1 subquery, batch insert, try/catch, 404s

Security:
- drift.ts: Fixed user.tenant_id and user.sub crash risk —
  replaced with user?.tenant_id ?? null and user?.sub ?? null in
  all 7+ occurrences.
- drift.ts: Added rateLimitWrite on POST /baselines, DELETE
  /baselines/:id, POST /scan, PUT /events/:id/resolve, PUT
  /events/:id/acknowledge (5 endpoints).
- shared-validation: Enhanced driftBaselineSchema and
  driftScanSchema — device_id max 200, name max 200.

Bug fixes:
- drift.ts: DELETE /baselines/:id now returns 404 if baseline not
  found.
- drift.ts: PUT /events/:id/resolve now returns 404 if event not
  found.
- drift.ts: PUT /events/:id/acknowledge now returns 404 if event
  not found.
- drift.ts: All audit logs migrated to writeAuditLog helper.

Performance:
- drift.ts: Fixed N+1 subquery in GET /baselines — replaced
  correlated subquery (open_drifts per row) with LEFT JOIN +
  aggregation.
- drift.ts: Parallelized 2 queries in GET / (baselines + events)
  using Promise.all.
- drift.ts: Parallelized 5 queries in GET /stats (total_baselines
  + open_drifts + critical_drifts + recent_drifts + by_device)
  using Promise.all — was 5 sequential queries.
- drift.ts: Replaced serial drift inserts in POST /scan with
  batch insert (single query with multi-VALUES) — was N queries
  for N drifts.
- drift.ts: Added httpCache(30) on GET /, GET /baselines, GET
  /events; httpCache(60) on GET /stats (4 endpoints).

Code quality:
- drift.ts: Added try/catch on all 8 endpoints with structured
  logging (was zero).
- drift.ts: Added logger with baselineId, device_id, name,
  driftsFound, eventId, tenantId context.

Tests:
- drift.test.ts (NEW): 20 tests covering:
  - driftBaselineSchema (7 tests): device_id required + max 200,
    name required + max 200, config_snapshot empty + nested.
  - driftScanSchema (4 tests): device_id required + max 200,
    current_config empty.
  - Config hash logic (3 tests): SHA-256 consistency, different
    configs different hashes, 64-char hex format.
  - Drift detection logic (6 tests): added key, removed key,
    modified key, identical configs, multiple simultaneous drifts,
    empty configs.

Total: 2347 tests passing (was 2327, +20 new).

Generated with [Devin](https://devin.ai)

Co-Authored-By: Devin <158243242+devin-ai-integration[bot]@users.noreply.github.com>
