feat(data-transfer): fix source_table regex, user?.sub, safeJsonBody, try/catch, parallelize

Security (CRITICAL):
- shared-validation/src/index.ts: Fixed createExportTemplateSchema —
  source_table was z.string().min(1) without regex, allowing SQL
  injection via template (later used in SELECT ... FROM public.${source_table}).
  Added regex /^[a-zA-Z_][a-zA-Z0-9_]*$/ to source_table and columns.
  Same fix applied to updateExportTemplateSchema.source_table and columns.
  Added new runImportSchema for validating import run body.
- data-transfer.ts: Fixed user.sub crash risk — replaced with user?.sub
  ?? null in all 6+ occurrences. All audit log calls now guarded with
  if (user?.sub).
- data-transfer.ts: Added rateLimitWrite on POST /templates,
  PUT /templates/:id, DELETE /templates/:id, POST /exports,
  DELETE /exports/:id, POST /imports, POST /imports/:id/run,
  DELETE /imports/:id (8 endpoints).
- data-transfer.ts: Replaced c.req.json() with safeJsonBody() in 5
  endpoints (POST /templates, PUT /templates/:id, POST /exports,
  POST /imports, POST /imports/:id/run) — was crashing on invalid JSON.
- data-transfer.ts: POST /imports/:id/run now validates body with
  runImportSchema (Zod) — was using raw c.req.json() without validation.

Bug fixes:
- data-transfer.ts: Fixed DELETE /templates/:id to verify template
  exists before deleting (was returning deleted:true regardless).
  Added RETURNING id + 404.
- data-transfer.ts: Fixed DELETE /exports/:id to verify export exists
  (was returning deleted:true regardless). Added RETURNING id + 404.
- data-transfer.ts: Fixed DELETE /imports/:id to verify import exists
  (was returning deleted:true regardless). Added RETURNING id + 404.
- data-transfer.ts: Fixed PUT /templates/:id to return 404 when
  template not found (added RETURNING id to UPDATE query).
- data-transfer.ts: Fixed POST /exports to verify INSERT succeeded
  (was continuing to export even if INSERT failed).
- data-transfer.ts: Fixed POST /imports to use data.name ?? fallback
  when name is not provided (was inserting undefined).

Performance:
- data-transfer.ts: Parallelized 5 queries in GET /stats using
  Promise.all (was 5 sequential queries).
- data-transfer.ts: Parallelized 2 queries in GET / (overview).
- data-transfer.ts: Added httpCache(30) on GET /, GET /templates,
  GET /stats; httpCache(60) on GET /whitelist; httpCache(15) on
  GET /exports, GET /imports (6 endpoints).

Code quality:
- data-transfer.ts: Added try/catch on all 12 endpoints with structured
  logging.
- data-transfer.ts: Added logger with templateId, exportId, importId,
  tenantId context. Logs create, update, delete, export, import.
- data-transfer.ts: Added validation error details (parsed.error.flatten())
  to all error responses.

Tests:
- data-transfer.test.ts (NEW): 73 tests covering all 5 Zod schemas
  (createExportTemplate, updateExportTemplate, createDataExport,
  createDataImport, runImport) with SQL injection prevention tests
  (source_table, columns, target_table), all format enums (csv/json/sql,
  csv/json/xml), defaults, constraints; import status logic
  (completed/partial/failed); CSV escape logic; SQL escape logic.

Total: 1097 tests passing (was 1024, +73 new).

Generated with [Devin](https://devin.ai)

Co-Authored-By: Devin <158243242+devin-ai-integration[bot]@users.noreply.github.com>
