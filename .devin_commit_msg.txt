feat(auth): fix safeJsonBody, try/catch, audit logs, Zod schemas oauth/ldap, logger

Security (CRITICAL):
- auth.ts: Added audit logs for login success, login failed (user_not_found,
  user_inactive, invalid_password, no_tenant_access), logout, oauth login,
  ldap login, ldap login failed. Before: zero audit logs for auth events.
  Now: 8 new audit log points with metadata (ip, userAgent, reason).
- auth.ts: Added try/catch around argon2.verify in /login and /change-password
  — was crashing on malformed password_hash. Now returns 401 gracefully.
- auth.ts: Replaced c.req.json() with safeJsonBody() in 6 endpoints
  (login, logout, revoke, change-password, forgot-password, reset-password,
  oauth/callback, ldap/bind) — was crashing on invalid JSON body.
- shared-validation/src/index.ts: Added oauthCallbackSchema (Zod) —
  oauth/callback was using raw c.req.json<{code, provider}>() without
  validation. Now validates code (required) and provider (enum: "google").
- shared-validation/src/index.ts: Added ldapBindSchema (Zod) —
  ldap/bind was using raw c.req.json<{username, password}>() without
  validation. Now validates username and password (both required, min 1).
- lib/audit.ts: Relaxed userId type from string to string | null —
  allows audit logging of failed logins where userId is unknown.

Bug fixes:
- auth.ts: POST /logout now writes audit log (was missing).
- auth.ts: POST /forgot-password now catches errors internally and still
  returns generic response (anti user-enumeration). Before: error would
  crash and reveal if email existed.
- auth.ts: POST /change-password now returns validation error details
  (parsed.error.flatten()) — was returning generic "Dados inválidos".
- auth.ts: POST /reset-password now returns validation error details.
- auth.ts: POST /oauth/callback now returns validation error details.
- auth.ts: POST /ldap/bind now returns validation error details.

Code quality:
- auth.ts: Added try/catch on all 14 endpoints with structured logging.
  Before: only 4 endpoints had try/catch (refresh, revoke, oauth/callback,
  ldap/bind). Now: all 14 endpoints are protected.
- auth.ts: Added logger with userId, ip, email, provider context. Logs
  login success, login failed (with reason), logout, password change,
  password reset, oauth login, ldap login.
- auth.ts: All audit log calls wrapped in try/catch — audit log failure
  no longer blocks the auth flow.

Tests:
- auth.test.ts (NEW): 56 tests covering all 7 Zod schemas (loginInput,
  refreshToken, changePassword, forgotPassword, resetPassword,
  oauthCallback, ldapBind) with all field validations, edge cases
  (min length 8 for passwords, min length 32 for reset token, email
  format, provider enum); IP extraction logic (single, multiple, spaces,
  missing); MFA challenge logic (UUID format, 5-minute expiry); token
  TTL logic (30 days); anti user-enumeration logic (generic responses);
  login status logic (active/inactive, tenant access, MFA); change
  password logic (verify, revoke sessions, must_change_password flag).

Total: 1143 tests passing (was 1097, +46 new).

Generated with [Devin](https://devin.ai)

Co-Authored-By: Devin <158243242+devin-ai-integration[bot]@users.noreply.github.com>
