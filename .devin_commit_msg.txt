feat(sla): fix IDOR, info disclosure, parallelize queries, add rate limiting

Security (CRITICAL):
- sla.ts: Fixed information disclosure — all DB error messages were
  leaked to client via `result.error.message`. Replaced with generic
  Portuguese messages. Affects 8+ endpoints.
- sla.ts: Fixed IDOR in POST /incidents — now verifies service_id
  belongs to tenant before creating incident (was missing, allowing
  incident creation on other tenants' services).
- sla.ts: Fixed crash risk — `user.tenant_id` was used directly
  instead of `user?.tenant_id ?? null`, causing crash if user is
  undefined. Fixed in all 15+ endpoints.
- sla.ts: Added rateLimitWrite on POST /services, PUT /services/:id,
  DELETE /services/:id, POST /incidents, PUT /incidents/:id,
  POST /maintenance, PUT /maintenance/:id, DELETE /maintenance/:id.
- shared-validation: Added enum validation for service status
  (operational, degraded, down, maintenance), incident status
  (open, investigating, resolved, closed), maintenance status
  (scheduled, active, completed, cancelled), and maintenance_type
  (scheduled, emergency, corrective). Added max length constraints.

Performance:
- sla.ts: Parallelized 2 sequential queries in GET / with Promise.all.
  Added httpCache(30).
- sla.ts: Parallelized 4 sequential queries in GET /dashboard with
  Promise.all (slaResult, incidentsResult, aggregateResult, trendResult).
  Added structured logging with durationMs.
- sla.ts: Added httpCache(30) on /services, httpCache(15) on /incidents,
  httpCache(30) on /maintenance.

Code quality:
- sla.ts: Added try/catch on all 15 endpoints with graceful error
  responses and structured logging via @repo/logger.
- sla.ts: Added logger with tenantId, serviceId, incidentId,
  maintenanceId, and durationMs context.

Tests:
- sla.test.ts (NEW): 64 tests covering all Zod schemas (createService,
  updateService, createMaintenanceWindow, updateMaintenanceWindow,
  createServiceIncident, updateServiceIncident, slaReportQuery),
  uptime calculation, MTTR calculation, weighted SLA by priority,
  and edge cases (empty tenant, feature flag, incident resolution).

Total: 447 tests passing (was 383, +64 new).

Generated with [Devin](https://devin.ai)

Co-Authored-By: Devin <158243242+devin-ai-integration[bot]@users.noreply.github.com>
