feat(scripts): fix user?.sub, safeJsonBody, try/catch, enhance 3 schemas, 404s

Security (CRITICAL):
- scripts.ts: Fixed user.sub crash risk — replaced with user?.sub ?? null
  in all 5+ occurrences (POST /, PUT /:id, DELETE /:id, POST /:id/execute).
- scripts.ts: Added rateLimitWrite on 4 write endpoints (POST /,
  PUT /:id, DELETE /:id, POST /:id/execute).
- scripts.ts: Replaced c.req.json() with safeJsonBody() in 3 endpoints
  (POST /, PUT /:id, POST /:id/execute) — was crashing on invalid JSON.
- shared-validation: Enhanced createScriptSchema — content max 100KB
  (was unbounded, DoS risk), allowed_hosts now uses hostnameSchema
  with IP/localhost rejection (was accepting any string including
  192.168.1.1, 169.254.169.254), max_concurrent_executions max 100,
  tags max 20 items max 50 chars, removed deprecated timeout field.
- shared-validation: Enhanced updateScriptSchema — same constraints
  as create, allowed_hosts with hostname validation.
- shared-validation: Enhanced executeScriptSchema — target_host now
  uses hostnameSchema (rejects IPs and localhost, SSRF prevention),
  target_device_id now UUID validated, args values max 500 chars.
- shared-validation: Added hostnameSchema with regex + refine that
  rejects IPv4 (N.N.N.N pattern), IPv6 (contains :), and localhost.

Bug fixes:
- scripts.ts: DELETE /:id now returns 404 if not found (was returning
  { deactivated: true } even when nothing was updated).
- scripts.ts: PUT /:id now checks rowCount for 404 after update.
- scripts.ts: All validation errors now return parsed.error.flatten()
  details (was generic "Dados inválidos").
- scripts.ts: All audit logs migrated to writeAuditLog helper.

Performance:
- scripts.ts: Parallelized 2 queries in GET / (count + data) using
  Promise.all (was 2 sequential queries).
- scripts.ts: Parallelized 2 queries in GET /:id (script + versions)
  using Promise.all (was 2 sequential queries).
- scripts.ts: Added httpCache(15) on GET /, GET /:id, GET /:id/executions
  (3 endpoints).

Code quality:
- scripts.ts: Added try/catch on all 7 endpoints with structured logging.
  Before: zero try/catch. Now: all 7 endpoints protected.
- scripts.ts: Added logger with scriptId, tenantId, executionId context.

Tests:
- scripts.test.ts (NEW): 49 tests covering:
  - createScriptSchema (21 tests): name, content max 100KB, language
    enum (3 values), timeout_seconds range, max_concurrent_executions
    max 100, allowed_hosts with hostname validation (rejects IPs,
    localhost), tags limits.
  - updateScriptSchema (10 tests): partial updates, content max,
    language enum, allowed_hosts validation, all fields.
  - executeScriptSchema (12 tests): script_id UUID, target_host
    hostname validation (rejects IPs, localhost), target_device_id
    UUID, args value max 500.
  - Limit pagination (4 tests): default 50, max 200, offset.
  - Version bump logic (3 tests): increment on content change,
    maintain on no change, first version.

Total: 1541 tests passing (was 1492, +49 new).

Generated with [Devin](https://devin.ai)

Co-Authored-By: Devin <158243242+devin-ai-integration[bot]@users.noreply.github.com>
