feat(finops): fix user?.sub, try/catch, schemas, parallelize, 404

Security:
- finops.ts: Fixed user.tenant_id and user.sub crash risk —
  replaced with user?.tenant_id ?? null and user?.sub ?? null in
  all 8+ occurrences.
- finops.ts: Added rateLimitWrite on POST /costs, POST
  /optimizations, PUT /optimizations/:id/status, PUT /budgets (4
  endpoints).
- shared-validation: Enhanced all 4 finops schemas with max
  length: finopsCostSchema (period_start/end max 50, category max
  100, resource_name max 200, resource_type max 100, currency max
  10, usage_unit max 50, source max 100), finopsOptimizationSchema
  (category max 100, resource_name max 200, title max 500,
  description max 10000, currency max 10), finopsBudgetSchema
  (category max 100, currency max 10, alert_threshold_pct 0-100
  range validation).

Bug fixes:
- finops.ts: PUT /optimizations/:id/status now returns 404 if
  optimization not found.
- finops.ts: All audit logs migrated to writeAuditLog helper (was
  only 1 inline audit log, now 3 endpoints with audit logs).

Performance:
- finops.ts: Parallelized 3 queries in GET /summary (by_month +
  by_category + total) using Promise.all — was 3 sequential
  queries.
- finops.ts: Parallelized 5 queries in GET /stats (total_cost +
  total_savings + implemented_savings + pending_optimizations +
  budget) using Promise.all — was 5 sequential queries.
- finops.ts: Added httpCache(60) on GET /, GET /summary, GET
  /budgets, GET /stats; httpCache(30) on GET /costs, GET
  /optimizations (6 endpoints).

Code quality:
- finops.ts: Added try/catch on all 9 endpoints with structured
  logging (was zero).
- finops.ts: Added logger with costId, optId, budgetId, category,
  title, status, month, year, tenantId context.

Tests:
- finops.test.ts (NEW): 33 tests covering:
  - finopsCostSchema (9 tests): period_start/end required,
    category required + max 100, resource_name max 200, currency
    max 10, complete cost.
  - finopsOptimizationSchema (7 tests): title required + max 500,
    effort enum (low/medium/high), invalid effort rejected,
    estimated_savings_annual.
  - finopsOptimizationStatusSchema (7 tests): all 5 status values,
    invalid status rejected, actual_savings_monthly.
  - finopsBudgetSchema (8 tests): month 1-12, year >= 2000,
    category, alert_threshold_pct 0-100 range, invalid values
    rejected.
  - Savings calculation logic (2 tests): annual = monthly * 12,
    default annual calculation.

Total: 2276 tests passing (was 2243, +33 new).

Generated with [Devin](https://devin.ai)

Co-Authored-By: Devin <158243242+devin-ai-integration[bot]@users.noreply.github.com>
