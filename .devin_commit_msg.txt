feat(marketplace): add requirePermission, try/catch, parallelize, 404s

Security:
- marketplace.ts: Added requirePermission("marketplace:read") on
  GET /apps and GET /apps/:slug — were completely public, exposing
  catalog + tenant install status to anyone.
- marketplace.ts: Fixed user.tenant_id and user.sub crash risk —
  replaced with user?.tenant_id ?? null and user?.sub ?? null in
  all 8+ occurrences.
- marketplace.ts: Added rateLimitWrite on POST /install, PUT
  /configure, PUT /activate, PUT /disable, DELETE /installs/:id
  (5 endpoints).
- marketplace.ts: Replaced c.req.json().catch() with safeJsonBody
  on POST /install.

Bug fixes:
- marketplace.ts: PUT /configure, PUT /activate, PUT /disable now
  return 404 if install not found (3 endpoints).
- marketplace.ts: DELETE /installs/:id now returns 404 if install
  not found.
- marketplace.ts: All audit logs migrated to writeAuditLog helper.

Performance:
- marketplace.ts: Parallelized 2 queries in GET /apps (apps list +
  tenant installs) using Promise.all — was 2 sequential queries.
- marketplace.ts: Parallelized 4 queries in GET /stats (total_apps
  + installed + active + by_category) using Promise.all — was 4
  sequential queries.
- marketplace.ts: Parallelized 3 operations in POST /install
  (increment count + audit log) using Promise.all.
- marketplace.ts: Parallelized 3 operations in DELETE /installs
  (delete + decrement count + audit log) using Promise.all.
- marketplace.ts: Added httpCache(60) on GET /, GET /apps, GET
  /apps/:slug, GET /stats; httpCache(30) on GET /installs (5
  endpoints).

Code quality:
- marketplace.ts: Added try/catch on all 9 endpoints with
  structured logging (was zero).
- marketplace.ts: Added logger with installId, appId, appSlug,
  tenantId context.

Tests:
- marketplace.test.ts (NEW): 17 tests covering:
  - marketplaceConfigureSchema (4 tests): empty config, with
    config, nested objects, null values.
  - Installed map logic (3 tests): map creation, installed check,
    install_status retrieval.
  - Install status logic (4 tests): all 4 valid statuses.
  - Default config logic (3 tests): body config, app default,
    empty fallback.
  - Installs count logic (3 tests): increment, decrement,
    GREATEST(0) non-negative.

Total: 2293 tests passing (was 2276, +17 new).

Generated with [Devin](https://devin.ai)

Co-Authored-By: Devin <158243242+devin-ai-integration[bot]@users.noreply.github.com>
