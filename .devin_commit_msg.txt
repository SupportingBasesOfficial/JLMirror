feat(lgpd): fix user?.sub, safeJsonBody, try/catch, schemas, parallelize

Security:
- lgpd.ts: Fixed user.sub crash risk — replaced with user?.sub ??
  null in all 4+ occurrences. Self-delete check now uses userId
  (from user?.sub ?? null) instead of user.sub directly.
- lgpd.ts: Added rateLimitWrite on POST /requests/export and POST
  /requests/delete.
- lgpd.ts: Replaced c.req.json() with safeJsonBody() in 2
  endpoints.
- shared-validation: Added lgpdExportSchema (user_id UUID, reason
  max 1000).
- shared-validation: Added lgpdDeleteSchema (user_id UUID, reason
  max 1000, mode enum anonymize/delete default anonymize).

Bug fixes:
- lgpd.ts: All validation errors now return parsed.error.flatten().
- lgpd.ts: All audit logs migrated to writeAuditLog helper.
- lgpd.ts: POST /requests/export and POST /requests/delete now
  use schemas instead of inline validation.

Performance:
- lgpd.ts: Parallelized 9 queries in POST /requests/export
  (user + sessions + devices + tenant_users + profiles + audit +
  security + contacts + company) using Promise.all — was 9
  sequential queries.
- lgpd.ts: Parallelized 5 deletes in POST /requests/delete
  (mode=delete) using Promise.all — was 5 sequential deletes.
- lgpd.ts: Parallelized 4 operations in POST /requests/delete
  (mode=anonymize) using Promise.all — was 4 sequential
  operations.
- lgpd.ts: Added httpCache(30) on GET /, GET /requests, GET
  /requests/:id (3 endpoints).

Code quality:
- lgpd.ts: Added try/catch on all 5 endpoints with structured
  logging (was only on export/delete inner blocks).
- lgpd.ts: Added logger with requestId, userId, mode, tenantId
  context.

Tests:
- lgpd.test.ts (NEW): 20 tests covering:
  - lgpdExportSchema (5 tests): user_id UUID required, reason max
    1000.
  - lgpdDeleteSchema (9 tests): user_id UUID required, mode enum
    (anonymize/delete), default anonymize, reason max 1000.
  - Self-delete protection logic (3 tests): blocks self-delete,
    allows other delete, null userId edge case.
  - Anonymization logic (3 tests): email generation, name, initials.

Total: 2196 tests passing (was 2176, +20 new).

Generated with [Devin](https://devin.ai)

Co-Authored-By: Devin <158243242+devin-ai-integration[bot]@users.noreply.github.com>
