feat(audit): fix try/catch, query param validation, tenant isolation, parallelize

Security (CRITICAL):
- audit.ts: Added tenant isolation on GET /logs — non-global users can
  only see logs from their own tenant. Before: any tenant user could
  query logs from any tenant by passing tenant_id query param.
- audit.ts: Added tenant isolation on GET /stats — non-global users
  only see stats for their own tenant. Before: any tenant user could
  see global audit stats.
- audit.ts: Added Zod validation for all query params (auditLogsQuerySchema)
  — user_id and tenant_id now validated as UUID, from/to as ISO datetime,
  action max 100 chars, limit 1-500, offset >= 0. Before: invalid params
  could cause DB errors or SQL injection via ILIKE.

Bug fixes:
- audit.ts: GET /logs now parallelizes data + count queries using
  Promise.all (was 2 sequential queries).
- audit.ts: All validation errors now return parsed.error.flatten()
  details (was generic error).

Performance:
- audit.ts: Added httpCache(60) on GET /, httpCache(15) on GET /logs,
  httpCache(30) on GET /stats (3 endpoints).

Code quality:
- audit.ts: Added try/catch on GET /logs and GET /stats with structured
  logging. Before: zero try/catch.
- audit.ts: Added logger with error context.

Tests:
- audit.test.ts (NEW): 36 tests covering auditLogsQuerySchema (all
  field validations: UUID, datetime, max length, coerce, defaults,
  limits); tenant isolation logic (global vs tenant scope, query
  tenant_id override); WHERE clause logic; pagination logic; stats
  scope logic.

Total: 1227 tests passing (was 1191, +36 new).

Generated with [Devin](https://devin.ai)

Co-Authored-By: Devin <158243242+devin-ai-integration[bot]@users.noreply.github.com>
