feat(workflows): fix user?.sub, safeJsonBody, try/catch, rate limit, 404s, clone schema

Security (CRITICAL):
- workflows.ts: Fixed user.sub crash risk — replaced with user?.sub ?? null
  in all 8+ occurrences (audit logs, INSERT created_by, triggered_by,
  approved_by). All audit log calls now guarded with if (user?.sub).
- workflows.ts: Added rateLimitWrite on POST /, PUT /:id, DELETE /:id,
  POST /:id/clone, POST /:id/execute, POST /executions/:executionId/cancel,
  POST /executions/:executionId/steps/:stepId/approve,
  POST /executions/:executionId/steps/:stepId/reject (8 endpoints).
- workflows.ts: Replaced c.req.json() with safeJsonBody() in 4 endpoints
  (POST /, PUT /:id, POST /:id/clone, POST /:id/execute,
  POST /executions/:executionId/steps/:stepId/reject) — was crashing on
  invalid JSON body.
- workflows.ts: Added cloneWorkflowSchema (Zod) — clone endpoint was using
  raw c.req.json() without validation. Now validates device_hostname
  (required), device_id (UUID optional), name (optional).
- workflows.ts: Added executeWorkflowSchema and rejectStepSchema — both
  were using raw c.req.json() without validation.

Bug fixes:
- workflows.ts: CRITICAL — GET / was registered twice (overview + list).
  In Hono, first registered wins, so listagem never worked. Front-end
  calls GET /api/v1/workflows?active=false expecting { workflows: [] }
  but got { overview, endpoints }. Moved overview to GET /overview,
  kept list at GET /. Now front-end listagem works.
- workflows.ts: Fixed DELETE /:id to verify workflow exists before
  deleting (was returning deleted:true regardless). Added RETURNING id
  + 404.
- workflows.ts: Fixed PUT /:id to return 404 when workflow not found
  (added RETURNING id to UPDATE query).
- workflows.ts: Fixed POST /executions/:executionId/steps/:stepId/reject
  — was missing audit log for rejection. Now logs
  'workflow.step.reject' with reason.

Performance:
- workflows.ts: Added httpCache(15) on GET /:id/executions (was missing).

Code quality:
- workflows.ts: Added try/catch on all 12 endpoints with structured
  logging.
- workflows.ts: Added logger with workflowId, executionId, stepId,
  tenantId context. Logs create, update, delete, clone, execute, cancel,
  approve, reject.
- workflows.ts: Added validation error details (parsed.error.flatten())
  to create and update error responses.

Tests:
- workflows.test.ts (NEW): 82 tests covering all 4 Zod schemas
  (createStep, createWorkflow, cloneWorkflow, executeWorkflow,
  rejectStep) with all enum values (stepType, language, onFailure,
  category), defaults, constraints (retry_count 0-10, timeout 0-86400,
  retry_delay 0-3600); execution status cancel logic; step approval
  logic; step status after cancel logic.

Total: 1024 tests passing (was 953, +71 new).

Generated with [Devin](https://devin.ai)

Co-Authored-By: Devin <158243242+devin-ai-integration[bot]@users.noreply.github.com>
