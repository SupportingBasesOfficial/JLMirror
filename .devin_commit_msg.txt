feat(tickets): fix IDOR, add rate limiting, parallelize stats, harden validation

Security (CRITICAL):
- tickets.ts: Fixed IDOR vulnerability in GET /:id/comments — query now
  filters by tenant_id (was only filtering by ticket_id, allowing
  cross-tenant comment access).
- tickets.ts: Added rateLimitWrite middleware on POST /, PUT /:id,
  DELETE /:id, POST /:id/comments, POST /categories (prevents spam/abuse).
- shared-validation: Fixed createCommentSchema — removed duplicate
  required field 'content' (was requiring both 'content' and 'body',
  causing validation failures). Added max(10000) to body to prevent
  oversized comments.

Performance:
- tickets.ts: Parallelized 5 sequential queries in GET /stats with
  Promise.all (latency reduction ~80%). Added httpCache(30) on /stats
  and httpCache(60) on /categories.

Code quality:
- tickets.ts: Added structured logging with @repo/logger on /stats.
- tickets.ts: Fixed fieldMap in PUT /:id — aligned 'assignee_id' (schema)
  with 'assigned_to' (DB column), removed 'assigned_name' (not in schema).
- tickets.ts: Added import for rateLimitWrite and httpCache middleware.

Frontend:
- tickets/page.tsx: Replaced silent console.error with user-facing
  setError() in handleSelectTicket, handleUpdateStatus, handleAddComment,
  handleDeleteCategory. Users now see error messages when operations fail.

Tests:
- tickets.test.ts (NEW): 42 tests covering all Zod schemas (createTicket,
  updateTicket, createComment, createCategory, updateCategory), SLA
  breach calculation, priority sorting, status logic, and edge cases
  (empty tenant, internal comments filtering, null category).

Total: 335 tests passing (was 293, +42 new).

Generated with [Devin](https://devin.ai)

Co-Authored-By: Devin <158243242+devin-ai-integration[bot]@users.noreply.github.com>
