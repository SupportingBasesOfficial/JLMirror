feat(mfa): fix rate limit, user?.sub, safeJsonBody, try/catch, schemas

Security:
- mfa.ts: Added rateLimitWrite on 4 endpoints (POST /setup,
  POST /setup/verify, POST /verify, POST /disable) — critical for
  POST /verify to prevent TOTP brute force (only 1M combinations).
- mfa.ts: Replaced c.req.json() with safeJsonBody() in 3 endpoints
  (POST /setup/verify, POST /verify, POST /disable).
- shared-validation: Enhanced mfaSetupVerifySchema — removed unused
  `secret` field (was not used by endpoint), added regex /^\d{6}$/
  to enforce exactly 6 digits (was accepting letters).
- shared-validation: Enhanced mfaVerifySchema — challenge_token max
  500, code max 20 with regex /^[\w-]+$/ (accepts recovery codes
  which are alphanumeric with hyphens).
- shared-validation: Added mfaDisableSchema with 6-digit regex
  validation (was inline regex in route).

Bug fixes:
- mfa.ts: All validation errors now return parsed.error.flatten().
- mfa.ts: All audit logs migrated to writeAuditLog helper.
- mfa.ts: POST /setup/verify and POST /disable now update
  users.mfa_enabled and users.mfa_method flags (was only updating
  user_mfa_totp table, leaving users table stale).
- auth-extended.test.ts: Updated mfaSetupVerifySchema tests to
  match new schema (removed `secret` field).

Performance:
- mfa.ts: Parallelized 2 queries in GET /status (totp + webauthn).
- mfa.ts: Added httpCache(15) on GET / and GET /status.

Code quality:
- mfa.ts: Added try/catch on all 6 endpoints with structured logging.
- mfa.ts: Added logger with userId context.

Tests:
- mfa.test.ts (NEW): 27 tests covering:
  - mfaSetupVerifySchema (7 tests): 6-digit validation, regex,
    empty, letters, spaces rejection.
  - mfaVerifySchema (9 tests): challenge_token required/max 500,
    code 6-20 chars, recovery code alphanumeric, special chars
    rejection.
  - mfaDisableSchema (5 tests): 6-digit validation, letters, empty.
  - TOTP code validation logic (3 tests): 6 digits valid, letters
    invalid, recovery code accepted by mfaVerifySchema.
  - Challenge expiry logic (3 tests): expired, valid, consumed.

Total: 1927 tests passing (was 1900, +27 new).

Generated with [Devin](https://devin.ai)

Co-Authored-By: Devin <158243242+devin-ai-integration[bot]@users.noreply.github.com>
