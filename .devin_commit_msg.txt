feat(chatops): add Teams token verification, mask bot_token, try/catch

Security (CRITICAL):
- chatops.ts: Added Teams webhook token verification — was
  completely unauthenticated, anyone could send commands as any
  tenant. Now checks x-teams-token header against
  teams_app_password if configured.
- chatops.ts: GET /config now masks slack_bot_token as "***"
  before returning — was leaking bot token in plaintext.
- chatops.ts: Fixed user.tenant_id and user.sub crash risk —
  replaced with user?.tenant_id ?? null and user?.sub ?? null.
- chatops.ts: Added rateLimitApi on both webhook endpoints (Slack
  + Teams) to prevent DoS via slash commands.
- chatops.ts: Added rateLimitWrite on PUT /config.
- shared-validation: Enhanced chatopsConfigSchema —
  slack_verification_token max 200, slack_signing_secret max 200,
  slack_bot_token max 500, teams_app_id max 200,
  teams_app_password max 500, enabled_commands items max 50 +
  array max 100.

Bug fixes:
- chatops.ts: All audit logs migrated to writeAuditLog helper.
- chatops.ts: Command log inserts wrapped in try/catch (was
  crashing silently if log table unavailable).

Performance:
- chatops.ts: Parallelized 5 queries in GET /stats (total +
  success + failed + byCommand + bySource) using Promise.all —
  was 5 sequential queries.
- chatops.ts: Added httpCache(60) on GET /; httpCache(15) on
  GET /config, GET /history; httpCache(30) on GET /stats (4
  endpoints).

Code quality:
- chatops.ts: Added try/catch on all 7 endpoints with structured
  logging.
- chatops.ts: Added logger with tenantId, platform, configId
  context.

Tests:
- chatops.test.ts (NEW): 23 tests covering:
  - chatopsConfigSchema (15 tests): platform enum (slack/teams),
    required platform, slack_verification_token max 200,
    slack_bot_token max 500, teams fields, enabled_commands items
    max 50, is_active boolean, complete config.
  - Command parsing logic (6 tests): simple command, command with
    args, empty, extra spaces, slash removal, no slash removal.
  - Token masking logic (2 tests): masks existing token, does not
    mask null.

Total: 2176 tests passing (was 2153, +23 new).

Generated with [Devin](https://devin.ai)

Co-Authored-By: Devin <158243242+devin-ai-integration[bot]@users.noreply.github.com>
