feat(k8s): fix Command Injection, user?.sub, safeJsonBody, try/catch, schemas

Security (CRITICAL):
- k8s.ts: Fixed Command Injection in execKubectl — was using exec()
  with shell interpolation of kubeconfigPath, context, and nsFlag.
  If kubeconfig_path contained '"; rm -rf /; "' or context contained
  '&& cat /etc/passwd', arbitrary commands would execute. Replaced
  exec() with execFile() (no shell) with args passed as array.
  Args are treated as string literals, not shell-interpreted.
- k8s.ts: Fixed user.sub crash risk — replaced with user?.sub ??
  null in all 4+ occurrences.
- k8s.ts: Added rateLimitWrite on 4 write endpoints (POST /clusters,
  PUT /clusters/:id, DELETE /clusters/:id, POST /:id/resources/:type/sync).
- k8s.ts: Replaced c.req.json() with safeJsonBody() in 2 endpoints
  (POST /clusters, PUT /clusters/:id).
- shared-validation: Enhanced updateK8sClusterSchema — was missing 5
  fields (display_name, api_server_url, context, namespace,
  kubeconfig_path, is_active). Now supports all fields with same
  validation as create (SAFE_PATH, SAFE_CONTEXT, namespace regex).

Bug fixes:
- k8s.ts: DELETE /clusters/:id now returns 404 if not found.
- k8s.ts: PUT /clusters/:id now returns 404 if not found, and 400 if
  no fields to update.
- k8s.ts: All validation errors now return parsed.error.flatten().
- k8s.ts: All audit logs migrated to writeAuditLog helper.
- k8s.ts: POST /clusters agora aceita api_server_url OU api_server
  (backward compatible).

Performance:
- k8s.ts: Parallelized 2 queries in GET /:clusterId/overview
  (resource_counts + event_counts) using Promise.all.
- k8s.ts: Added httpCache(30) on GET /, GET /clusters; httpCache(15)
  on GET /:id/resources/:type, GET /:id/events, GET /:id/overview
  (5 endpoints).

Code quality:
- k8s.ts: Added try/catch on all 8 endpoints with structured logging.
- k8s.ts: Added logger with clusterId, resourceType, tenantId context.

Tests:
- k8s.test.ts (NEW): 43 tests covering:
  - createK8sClusterSchema (15 tests): name, api_server URL, context
    SAFE_CONTEXT regex (rejects injection, spaces), kubeconfig_path
    SAFE_PATH regex (rejects injection, spaces), namespace regex,
    token, ca_cert.
  - updateK8sClusterSchema (11 tests): partial updates, all 5 new
    fields, api_server_url validation, context injection rejection.
  - k8sResourceTypeSchema (8 tests): pods/services/deployments/nodes/
    namespaces valid, invalid types rejected, injection rejected.
  - execKubectl Command Injection prevention (5 tests): args as array
    not shell interpolation, null kubeconfig/context handling,
    namespace flag logic.
  - Limit/offset pagination (4 tests).

Total: 1877 tests passing (was 1834, +43 new).

Generated with [Devin](https://devin.ai)

Co-Authored-By: Devin <158243242+devin-ai-integration[bot]@users.noreply.github.com>
