feat(api-keys): fix user?.sub, safeJsonBody, try/catch, enhance schemas, 404s

Security:
- api-keys.ts: Fixed user.sub crash risk — replaced with user?.sub ??
  null in all 4+ occurrences (POST /, PUT /:id, DELETE /:id,
  POST /:id/rotate).
- api-keys.ts: Added rateLimitWrite on 4 write endpoints (POST /,
  PUT /:id, DELETE /:id, POST /:id/rotate).
- api-keys.ts: Replaced c.req.json() with safeJsonBody() in 2 endpoints
  (POST /, PUT /:id).
- shared-validation: Enhanced createApiKeySchema — name max 200,
  description max 2000, scopes max 50 items max 100 chars each,
  allowed_ips now uses ipAddressSchema (IPv4/CIDR format validation,
  rejects hostnames and IPv6), rate_limit max values enforced.
- shared-validation: Enhanced updateApiKeySchema — was missing 5
  fields (description, rate_limit_per_min/hour/day, expires_at).
  Now supports all fields with same validation as create.
- shared-validation: Added ipAddressSchema with IPv4/CIDR regex.

Bug fixes:
- api-keys.ts: DELETE /:id now returns 404 if not found.
- api-keys.ts: PUT /:id now returns 404 if not found, and 400 if
  no fields to update.
- api-keys.ts: All validation errors now return parsed.error.flatten().
- api-keys.ts: All audit logs migrated to writeAuditLog helper.
- api-keys.ts: allowed_ips now stored as null when undefined (was
  storing "undefined" string via JSON.stringify).

Performance:
- api-keys.ts: Parallelized 3 queries in GET /stats (overview,
  top_keys, recent_usage) using Promise.all.
- api-keys.ts: Added httpCache(15) on GET /, GET /stats,
  GET /:id/usage (3 endpoints).

Code quality:
- api-keys.ts: Added try/catch on all 7 endpoints with structured
  logging.
- api-keys.ts: Added logger with keyId, tenantId context.

Tests:
- api-keys.test.ts (NEW): 45 tests covering:
  - createApiKeySchema (21 tests): name, scopes limits, description,
    expires_at, allowed_ips IPv4/CIDR validation (rejects hostnames,
    IPv6, invalid), rate_limit ranges.
  - updateApiKeySchema (10 tests): partial updates, all new fields,
    allowed_ips validation.
  - Key generation logic (8 tests): jl_ prefix, 67 char key, 12 char
    prefix, 64 char sha256 hash, hex validation, uniqueness,
    determinism.
  - Limit pagination (3 tests): default 50, max 200.
  - Rotation logic (3 tests): creates new + deactivates old,
    rotated_from linkage.

Total: 1739 tests passing (was 1694, +45 new).

Generated with [Devin](https://devin.ai)

Co-Authored-By: Devin <158243242+devin-ai-integration[bot]@users.noreply.github.com>
