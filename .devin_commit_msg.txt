feat(compliance): fix user?.sub, safeJsonBody, try/catch, schemas, parallelize

Security:
- compliance.ts: Fixed user.sub crash risk — replaced with
  user?.sub ?? null in all 8+ occurrences.
- compliance.ts: Added rateLimitWrite on 5 write endpoints (POST
  /policies, PUT /policies/:id, DELETE /policies/:id, POST /scans,
  PUT /violations/:id).
- compliance.ts: Replaced c.req.json() with safeJsonBody() in 4
  endpoints.
- shared-validation: Fixed createPolicySchema — removed phantom
  `requirements` field (was in schema but never used in INSERT),
  added severity enum (low/medium/high/critical), rule_type enum
  (manual/automated/scheduled), name max 200, description max
  5000, framework max 100, policy_category max 100,
  check_interval_hours max 720.
- shared-validation: Fixed updatePolicySchema — was missing 5
  fields (framework, policy_category, severity, rule_type,
  check_interval_hours) that were in the route's fieldMap. Now
  includes all fields with same validation as create.
- shared-validation: Added createScanSchema (policy_id UUID).
- shared-validation: Added updateViolationSchema (status enum:
  open/acknowledged/remediated/false_positive/wont_fix).

Bug fixes:
- compliance.ts: All validation errors now return
  parsed.error.flatten().
- compliance.ts: PUT /policies/:id and DELETE /policies/:id now
  return 404 if not found.
- compliance.ts: PUT /violations/:id now returns 404 if not found.
- compliance.ts: All audit logs migrated to writeAuditLog helper.
- compliance.ts: POST /scans now uses createScanSchema instead of
  inline validation.
- compliance.ts: PUT /violations/:id now uses updateViolationSchema
  instead of inline validation.

Performance:
- compliance.ts: Parallelized 4 queries in GET /stats (overview +
  violations + framework + scans) using Promise.all — was 4
  sequential queries.
- compliance.ts: Parallelized all compliance checks in POST /scans
  (MFA + SSL + firewall + backup + audit) using Promise.all — was
  5 sequential queries.
- compliance.ts: Parallelized 2 queries in POST /scans (update
  scan + update policy last_scanned) using Promise.all.
- compliance.ts: Added httpCache(30) on GET /, GET /policies, GET
  /scans, GET /violations; httpCache(60) on GET /stats (5
  endpoints).

Code quality:
- compliance.ts: Added try/catch on all 10 endpoints with
  structured logging.
- compliance.ts: Added logger with policyId, scanId, violationId,
  tenantId, score context.

Tests:
- compliance.test.ts (NEW): 37 tests covering:
  - createPolicySchema (16 tests): name required + max 200,
    framework required, severity enum, rule_type enum,
    check_interval_hours 1-720, is_active default, rule_config.
  - updatePolicySchema (7 tests): partial updates, severity enum
    validation, framework, check_interval_hours.
  - createScanSchema (3 tests): policy_id UUID required.
  - updateViolationSchema (7 tests): all 5 status values, invalid
    status rejected, missing status rejected.
  - Compliance score logic (4 tests): 100%, 0%, 50%, total 0.

Total: 2153 tests passing (was 2116, +37 new).

Generated with [Devin](https://devin.ai)

Co-Authored-By: Devin <158243242+devin-ai-integration[bot]@users.noreply.github.com>
