feat(docs): add CSP, rate limiting, logger, Cache-Control, fix escapeHtml

Security:
- docs.ts: Added Content-Security-Policy header on /ui endpoint to
  prevent XSS. Allows inline scripts/styles (required for interactive
  UI) but restricts connect-src to 'self' (no external fetch).
- docs.ts: Added X-Content-Type-Options: nosniff and Referrer-Policy
  headers on /ui endpoint.
- docs.ts: Added rate limiting (30 req/min) on GET / and GET /ui to
  protect against brute force of DOCS_PASSWORD.
- docs.ts: Fixed escapeHtml in Swagger UI — was not escaping single
  quotes ('), which could allow XSS in onclick attributes. Added
  replacement of ' with &#39;.
- docs.ts: Added logger.warn for failed auth attempts with IP and path
  context for security auditing.

Performance:
- docs.ts: Added Cache-Control: private, max-age=300 on GET / (spec
  JSON) to avoid re-serialization on every request.

Code quality:
- docs.ts: Specified explicit "utf8" encoding on Buffer.from() calls
  in checkDocsAuth to avoid encoding ambiguity.
- docs.ts: Added charset=utf-8 to Content-Type on /ui endpoint.

Tests:
- docs.test.ts (NEW): 35 tests covering checkDocsAuth logic (correct
  credentials, wrong password, missing header, non-Basic scheme,
  missing DOCS_PASSWORD, empty user, timing-safe length difference,
  invalid base64, special characters, unicode), escapeHtml (all
  special chars including single quote, XSS payloads, type coercion),
  OpenAPI spec structure, security headers (CSP, Cache-Control), rate
  limiting config, and edge cases (logout without auth, logout-clear
  401, long password rejection).

Total: 569 tests passing (was 534, +35 new).

Generated with [Devin](https://devin.ai)

Co-Authored-By: Devin <158243242+devin-ai-integration[bot]@users.noreply.github.com>
