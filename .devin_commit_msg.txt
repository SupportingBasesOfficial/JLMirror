feat(capacity): fix user?.sub, safeJsonBody, try/catch, batch insert, schemas

Security:
- capacity.ts: Fixed user.sub crash risk — replaced with user?.sub ??
  null in all 3+ occurrences (POST /thresholds, POST /reports).
- capacity.ts: Added rateLimitWrite on 6 write endpoints (POST /metrics,
  POST /metrics/batch, POST /thresholds, PUT /thresholds/:id,
  DELETE /thresholds/:id, POST /reports, DELETE /reports/:id).
- capacity.ts: Replaced c.req.json() with safeJsonBody() in 4 endpoints.
- shared-validation: Enhanced createThresholdSchema — warning_pct and
  critical_pct now validated 0-100 range, added refine() to enforce
  critical_pct > warning_pct (was accepting any number).
- shared-validation: Enhanced createReportSchema — report_type now
  enum (capacity_summary, trend_analysis, forecast,
  utilization_breakdown) instead of free string, name max 200,
  type max 100, cron_expression max 100.
- shared-validation: Enhanced ingestMetricSchema — all string fields
  now have max length, utilization_pct 0-100, max_capacity min 0.
- shared-validation: Reduced ingestMetricsBatchSchema max from 10000
  to 1000 (was DoS risk with serial loop).

Performance:
- capacity.ts: Replaced serial loop in POST /metrics/batch with
  multi-value INSERT (VALUES ($1,...), ($N,...)) in batches of 100.
  Before: 1000 individual INSERT queries. Now: 10 batched queries.
- capacity.ts: Parallelized 2 queries in GET / (metrics + thresholds).
- capacity.ts: Parallelized 4 queries in GET /stats (overview,
  by_type, top_utilized, forecasts) using Promise.all.
- capacity.ts: Added httpCache(30) on GET /, GET /thresholds,
  GET /reports; httpCache(15) on GET /metrics, GET /forecast,
  GET /stats.

Bug fixes:
- capacity.ts: DELETE /thresholds/:id now returns 404 if not found.
- capacity.ts: DELETE /reports/:id now returns 404 if not found.
- capacity.ts: PUT /thresholds/:id now returns 404 if not found,
  and 400 if no fields to update.
- capacity.ts: All validation errors now return parsed.error.flatten().
- capacity.ts: All audit logs migrated to writeAuditLog helper.
- capacity.ts: hours validation in GET /metrics (NaN/negativo -> 24,
  max 720).

Code quality:
- capacity.ts: Added try/catch on all 11 endpoints with structured
  logging.
- capacity.ts: Added logger with tenantId, reportId context.

Tests:
- capacity.test.ts (NEW): 73 tests covering:
  - ingestMetricSchema (13 tests): required fields, max_capacity
    range, utilization_pct 0-100, metadata, resource_id.
  - ingestMetricsBatchSchema (7 tests): min 1, max 1000, empty
    rejection, invalid metric rejection, non-array rejection.
  - createThresholdSchema (12 tests): warning/critical 0-100 range,
    critical > warning enforcement, defaults, boundaries.
  - updateThresholdSchema (7 tests): partial updates, range
    validation.
  - createReportSchema (16 tests): format enum, report_type enum
    (4 values), defaults, template_id UUID, date_range.
  - Hours validation (7 tests): default, NaN, zero, negative, max.
  - Batch insert logic (4 tests): BATCH_SIZE=100 division.
  - Report type selection (4 tests): each type generates correct
    summary key.
  - Limit pagination (2 tests).

Total: 1694 tests passing (was 1621, +73 new).

Generated with [Devin](https://devin.ai)

Co-Authored-By: Devin <158243242+devin-ai-integration[bot]@users.noreply.github.com>
